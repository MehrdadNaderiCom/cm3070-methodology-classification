# Repository status

This directory is the public GitHub payload.

1. Create an empty public repository (do not auto-add a README).
2. Push this folder as the repository root.
3. Tag `v1.0-final`.
4. Open the URL logged-out and clone it.
5. Put that URL on the report title page only after step 4 succeeds.
